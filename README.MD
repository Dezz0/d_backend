# Создание пользователя
```docker exec -it smart_home_backend python -m scripts.create_user```

# Пересобрать образ
```docker compose build [название]```

# Вход внутрь контейнера
```docker exec -it [название] sh```